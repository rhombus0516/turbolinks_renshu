//let promptStr = prompt('何か好きな文字を入力してください。');
//alert(promptStr);

let alertStering;
alertStering = addString("webcamp");
alert(alertStering);

function addString(strA){
    let addStr = "Hello" + strA;
    return addStr;
}